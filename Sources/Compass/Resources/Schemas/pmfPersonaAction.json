{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "PMFPersonaActionResponse",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "actionId",
    "params",
    "rationale",
    "expectation",
    "confusion"
  ],
  "properties": {
    "actionId": {
      "type": "string",
      "minLength": 1,
      "description": "Exact id of one currently allowed semantic app action."
    },
    "params": {
      "type": "object",
      "description": "Parameters for the selected semantic action. Use an empty object when none are needed.",
      "additionalProperties": true
    },
    "rationale": {
      "type": "string",
      "minLength": 1,
      "description": "Skeptical persona reason for choosing this action now."
    },
    "expectation": {
      "type": "string",
      "minLength": 1,
      "description": "What the persona expects the app to prove or reveal next."
    },
    "confusion": {
      "description": "Specific confusion from the current state, or null when nothing is confusing.",
      "anyOf": [
        {
          "type": "string",
          "minLength": 1
        },
        {
          "type": "null"
        }
      ]
    }
  }
}
