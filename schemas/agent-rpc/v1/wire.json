{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://compass.local/schemas/agent-rpc/v1/wire.json",
  "title": "Compass AgentRPC v1 wire compatibility marker",
  "description": "Compass AgentRPC uses a 4-byte big-endian length prefix followed by an externally-tagged JSON enum. Swift CompassGuestAgent and Rust compass-guest-agent must preserve these case names.",
  "type": "object",
  "required": ["schema_version"],
  "properties": {
    "schema_version": {
      "const": 1
    }
  }
}
